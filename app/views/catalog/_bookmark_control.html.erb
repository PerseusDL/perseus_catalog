<!-- Remove this override, the saved_searches override, the search_history override, the _user_util_links override, and the comment on :registration in models/user.rb to regain devise user functionality -->
<% if current_or_guest_user %>

<% else %>
  &nbsp;
<% end %>